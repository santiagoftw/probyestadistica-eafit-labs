---
title: "Ejercicios propuestos · Módulo 2 (Probabilidad)"
---

::: {.hero-motivacion}
## 🏋️ Ejercicios para resolver por tu cuenta

Estos son ítems reales de tus Laboratorios 2 y 3 que **no** se usaron como ejemplo guiado en ningún tema — quedaron para que los resuelvas de forma independiente, con o sin R.
:::

::: {.callout-note}
## Cómo usar esta página
Resuelve primero el cálculo teórico a mano; verifica después con una simulación en R, como practicaste en cada tema.
:::

---

## Tema P1 · Reglas básicas de probabilidad

**Fuente:** Laboratorio 2, Bloque 1.

1. **(Regla de adición en mantenimiento):** una fábrica de maquinaria tiene dos tipos de defectos posibles: Defecto A (falla de lubricación, $P(A)=0.15$) y Defecto B (falla eléctrica, $P(B)=0.10$), con $P(A\cap B)=0.05$. Calcula $P(A\cup B)$: la probabilidad de que ocurra al menos uno de los dos defectos.

2. **(Complemento en fiabilidad):** la probabilidad de que un componente mecánico funcione sin fallos durante su vida útil es 0.85. ¿Cuál es la probabilidad de que falle?

3. **(Diferencia simétrica en control de calidad):** un producto puede tener Defecto A ($P(A)=0.3$) o Defecto B ($P(B)=0.4$), con $P(A\cap B)=0.1$. Calcula la probabilidad de que el producto tenga **exactamente uno** de los dos defectos (no ambos), y también la probabilidad de que tenga **al menos uno**.

::: {.callout-important}
## Ejercicio de detección de errores (nivel avanzado)
Un problema real de este laboratorio plantea dos eventos con $\pi_1=0.4$, $\pi_2=0.5$, $P(A\cap B)=0.2$, y pide "la probabilidad de que al menos uno de los dos eventos ocurra". El código original de la solución calculó `1 - (p1+p2-p12)`. **Antes de aceptar ese resultado:** deriva tú mismo la fórmula correcta para $P(A\cup B)$ y compárala con lo que calcula esa línea de código. ¿Coinciden? Si no, ¿qué cantidad calculó realmente esa línea? Esta es una aplicación directa del "truco de interpretación" de no confiar ciegamente en un resultado sin verificar la fórmula que lo produjo.
:::

---

## Tema P3 · Probabilidad total y Bayes

**Fuente:** Laboratorio 2, Bloque 2.

4. Considera dos eventos $A$ y $B$ con $P(A)=0.4$, $P(B)=0.5$, $P(A\cap B)=0.2$. (a) Verifica si $A$ y $B$ son independientes. (b) Calcula la probabilidad de que al menos uno de los dos ocurra.

---

## Tema P5 · Variables aleatorias y FDA

**Fuente:** Laboratorio 2, Bloque 3.

5. Encuentra la expresión de la función de distribución empírica de cada conjunto de datos, y grafícala: (a) $\{2, 5\}$; (b) $\{-1, 0, 1\}$; (c) $\{4, 10, 10, 4, 10, 4\}$.

6. **(Análisis de riesgo):** tres fuentes de energía tienen probabilidad de falla y costo adicional en caso de falla: Fuente X ($P=0.2$, costo adicional \$1000, costo base \$20000); Fuente Y ($P=0.1$, costo adicional \$1500, costo base \$25000); Fuente Z ($P=0.15$, costo adicional \$1200, costo base \$22000). Simula 10,000 escenarios para cada fuente y determina el costo total esperado. ¿Cuál fuente tiene el mayor costo esperado?

---

## Tema P7 · Distribuciones continuas

**Fuente:** Laboratorio 2, Bloque 5.

7. **(Resistencia a la tracción):** en un laboratorio de materiales, la resistencia a la tracción de un nuevo material sigue una distribución normal con $\mu=500$ y $\sigma=50$. Determina: (a) la probabilidad de que una muestra tenga resistencia menor a 450; (b) la probabilidad de que la resistencia esté entre 450 y 550.

8. Estima la media y la varianza de una distribución uniforme continua en $[10, 20]$ a partir de una muestra simulada de tamaño $n=1000$. Compara contra los valores teóricos $\frac{a+b}{2}$ y $\frac{(b-a)^2}{12}$.

---

## Tema P8 · Distribución multivariada

**Fuente:** Laboratorio 3, Bloque 1.

9. Retoma la tabla de frecuencias conjuntas:

| x \\ y | 10 | 20 | 30 | 40 |
|---|---|---|---|---|
| 0 | 7 | 4 | 1 | 4 |
| 1 | 3 | 2 | 8 | 2 |
| 2 | 0 | 2 | 4 | 6 |
| 3 | 4 | 1 | 0 | 4 |

Encuentra las frecuencias marginales de $x$ y de $y$ por separado (sin usar `rowSums()`/`colSums()` primero — hazlo sumando a mano, y verifica después en R).

---

## Tema P9 · Covarianza y correlación de variables aleatorias

**Fuente:** Laboratorio 3, Bloque 2.

10. Con el ejemplo de resistencia a la tracción (R, $\mu=500$, $\sigma=50$) y módulo de elasticidad (E, $\mu=200$, $\sigma=20$), correlación 0.6, y vida útil $L=R^2/E$: en vez de simular con `mvrnorm()`, escribe primero (en papel) qué esperarías que pase con la correlación entre $L$ y $R$ si la correlación entre $R$ y $E$ fuera 0 en vez de 0.6. Luego verifica tu intuición cambiando el parámetro y simulando.

---

::: {.callout-tip}
## 💡 Truco de interpretación
Varios de estos ejercicios piden "verifica por simulación" — no te saltes el cálculo teórico previo. La simulación confirma (o refuta) tu razonamiento; no lo reemplaza. [Ver más trucos →](../recursos.qmd)
:::
